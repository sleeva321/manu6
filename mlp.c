#include<stdio.h>
int main()
{
                int x,i,mul;
 
 
                scanf("%d",&x);
                for(i=1;i<=5;i++)
                {
                    mul=x*i;
                               printf(" %d",mul);
                 }
 
}
